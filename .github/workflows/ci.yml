name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  linux:
    name: Linux / HEMTT
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"

      - name: Set up HEMTT
        uses: arma-actions/hemtt@a0570ddd80d2b7be9c0f052494c0f700b42668dd # v1
        with:
          annotate: true
          version: "v1.21.0"

      - name: Validate repository
        run: python -B tools/validate_repository.py

      - name: Run tests
        run: python -B -m unittest discover -s tests -p "test_*.py" -v

      - name: HEMTT check
        run: hemtt check

      - name: HEMTT build without binarization
        run: hemtt build --no-bin

      - name: Upload Linux build
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: ${{ github.event.repository.name }}-${{ github.sha }}-linux
          path: .hemttout/build/**
          include-hidden-files: true
          if-no-files-found: error

  windows:
    name: Windows / HEMTT
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"

      - name: Set up HEMTT
        uses: arma-actions/hemtt@a0570ddd80d2b7be9c0f052494c0f700b42668dd # v1
        with:
          annotate: true
          version: "v1.21.0"

      - name: Set up Arma 3 Tools
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.BUILD_ASSETS_TOKEN }}
        run: ./tools/setup_arma3_tools.ps1

      - name: Validate repository
        run: python -B tools/validate_repository.py

      - name: Run tests
        run: python -B -m unittest discover -s tests -p "test_*.py" -v

      - name: Validate release scripts
        shell: pwsh
        run: |
          Get-ChildItem tools -Filter *.py | ForEach-Object {
              python -m py_compile $_.FullName
              if ($LASTEXITCODE -ne 0) { throw "Invalid Python: $($_.Name)" }
          }

          Get-ChildItem tools -Filter *.ps1 | ForEach-Object {
              $tokens = $null
              $parseErrors = $null
              [System.Management.Automation.Language.Parser]::ParseFile(
                  $_.FullName,
                  [ref]$tokens,
                  [ref]$parseErrors
              ) | Out-Null
              if ($parseErrors.Count -gt 0) {
                  $parseErrors | ForEach-Object { Write-Error $_.Message }
                  throw "Invalid PowerShell: $($_.Name)"
              }
          }

      - name: HEMTT check
        run: hemtt check

      - name: HEMTT build
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: hemtt build

      - name: HEMTT build without binarization
        if: github.event_name != 'push' || github.ref != 'refs/heads/main'
        run: hemtt build --no-bin

      - name: Upload Windows build
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: ${{ github.event.repository.name }}-${{ github.sha }}-windows
          path: .hemttout/build/**
          include-hidden-files: true
          if-no-files-found: error
